# This file is used to configure the LittlelemonAPI app.
# An AppConfig is a Python class that stores metadata about an application.

from django.apps import AppConfig


class LittlelemonapiConfig(AppConfig):
    # The name attribute is the full Python path to the application.
    name = 'LittlelemonAPI'